// frontend/next.config.js
// Minimal Next.js config for now. We'll add image domains, redirects,
// and API rewrites in later phases as needed.

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = nextConfig;