# scanner-worker/requirements.txt
# Phase 2 expands the worker beyond a heartbeat - now it needs DB access
# to update scan status, and git which is already installed in the Dockerfile.

redis==5.0.8
python-dotenv==1.0.1
sqlalchemy==2.0.35
psycopg2-binary==2.9.9